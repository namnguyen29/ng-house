import { Environment } from '@app-shared/interfaces';

export const environment: Environment = {
  baseUrl: 'http://localhost:3000/locations',
  production: false,
  jsonApiUrl: 'https://jsonplaceholder.typicode.com/todos/'
};
